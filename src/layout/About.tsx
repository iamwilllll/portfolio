import { Container } from '../components';

export function About({ className }: { className?: string }) {
    return (
        <Container className={className} id="about">
            <></>
        </Container>
    );
}
